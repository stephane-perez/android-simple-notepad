Add one file per release here, named after that release's `versionCode`
(e.g. `17.txt`), containing a short plain-text changelog entry. F-Droid
displays it on the app's page for that version. Optional, but nice to have.
